import { RemoveDashesPipe } from './remove-dashes.pipe';

describe('RemoveDashesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDashesPipe();
    expect(pipe).toBeTruthy();
  });
});
